{
  "name": "BookList",
  "region": "eu-west-1",
  "plan": "dev",
  "buildpacks": [{ "url": "https://github.com/Scalingo/multi-buildpack.git" }],
  "formation": {
    "web": {
      "size": "1:Small",
      "quantity": 1
    }
  },
  "env": {
    "NODE_ENV": "production"
  },
  "scripts": {
    "build": "cd client && npm install && npm run build && cd ../api && npm install && npm run build",
    "start": "cd client && npm install && npm run build && cd ../api && npm install && npm run start"
  },
  "predeploy": []
}
